[Unit]
Description=Log Search
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/log-search
ExecStart=/opt/log-search/log-search --config /opt/log-search/config.toml --static-dir /opt/log-search/frontend
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
